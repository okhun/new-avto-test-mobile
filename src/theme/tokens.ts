export type ThemeTokens = {
  background: string;
  foreground: string;
  card: string;
  muted: string;
  primary: string;
  secondary: string;
};

export type ThemePalette = {
  light: ThemeTokens;
  dark: ThemeTokens;
};
