// Keep the Reanimated Babel plugin enabled to support future theme animations.
// This file is a marker to centralize any reanimated-specific setup later.
export const reanimatedConfig = {
  pluginRequired: true,
};
