/**
 * Backwards-compatible re-exports. Prefer importing from `@/src/theme`.
 */
export {
  ThemeProvider,
  useResolvedTheme,
  useSystemColorScheme,
  useTheme,
  useThemePreference,
} from "@/src/theme";
export type {
  ResolvedTheme,
  ThemePreference,
  UseThemeResult,
} from "@/src/theme";
